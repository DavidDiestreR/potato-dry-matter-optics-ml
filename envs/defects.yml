name: potato-detection
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - jupyterlab
  - notebook
  - ipykernel
  - numpy
  - pillow # AÑADIR AL ENV GENERAL
  - matplotlib
  - pip:
      - inference          # SDK de Roboflow para correr modelos en local/hosted | AÑADIR AL ENV GENERAL

      # ELIMINAR
      - supervision        # Para visualizar resultados si quieres
      - opencv-python      # Para dibujar cajas, lectura de imagen, etc.